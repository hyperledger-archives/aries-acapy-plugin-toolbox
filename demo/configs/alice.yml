label: Alice
invite-label: Alice (Admin)
wallet-name: alice
endpoint:
  - http://localhost:3000
  - ws://localhost:3000
inbound-transport:
  - [acapy_plugin_toolbox.http_ws, 0.0.0.0, 3000]
admin: [0.0.0.0, 3001]
